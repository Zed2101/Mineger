## What this changes

<!-- One paragraph: what and why. Link the issue if there is one. -->

## How it was tested

<!-- e.g. cargo test --lib, started a Forge 1.20.1 server, installed a Modrinth pack, tried the remote host from a second PC -->

## Checklist

- [ ] `cargo test --lib` passes
- [ ] `CHANGELOG.md` has a line under **Unreleased** (if users would notice the change)
- [ ] New strings are in `src/language/it.json`, `en.json` and `en-us.json` (`npm run lang:template` to check)
