# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
servers/*
# Tailwind output (generato da `npm run css:build`)
src/tailwind.css

# Impostazioni locali (host remoto, token)
src-tauri/src/data/settings.json

# Icone caricate dall utente (dev)
/icons/

# Piano di lavoro interno
PIANO.md

# Chiavi di firma dell'updater (vivono in ~/.tauri, mai nel repo)
*.key
*.key.pub
*.key.password

# Agent playit.gg compilato dal sorgente (`npm run playit:build`)
src-tauri/binaries/

# Sito (GitHub Pages): CSS compilato e release.json generati in CI
site/tailwind.css
site/release.json

# Agent worktrees (Claude Code)
.claude/worktrees/
# JRE Temurin scaricate dall'app in dev (`<repo>/java`, vedi paths::java_dir)
/java/
